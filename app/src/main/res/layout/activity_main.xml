<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.example.task3.AnimatedButtonView
        android:id="@+id/animatedButton"
        android:layout_width="300dp"
        android:layout_height="120dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/redButton1"
        style="@style/RedButtonStyle"
        android:layout_marginBottom="1dp"
        android:text="@string/_1"
        app:layout_constraintBottom_toTopOf="@id/animatedButton"
        app:layout_constraintEnd_toEndOf="@id/animatedButton"
        app:layout_constraintStart_toStartOf="@id/animatedButton" />

    <TextView
        android:id="@+id/redButton2"
        style="@style/RedButtonStyle"
        android:layout_marginBottom="20dp"
        android:text="@string/_2"
        app:layout_constraintBottom_toTopOf="@id/redButton1"
        app:layout_constraintEnd_toEndOf="@id/redButton1"
        app:layout_constraintStart_toStartOf="@id/redButton1" />

</androidx.constraintlayout.widget.ConstraintLayout>
